<?php 
session_start();
include("./auth.php");
$isLoggedIn = isset($_SESSION['auth']);
?>

<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>OTTBUY - Premium Account Shop</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.2/css/all.min.css">
    <style>
        body { background-color: #f8f9fa; color: #212529; }
        .navbar { background: #343a40; padding: 15px; }
        .navbar-brand img { height: 50px; }
        .card { background-color: #fff; border-radius: 12px; transition: 0.3s; }
        .card:hover { box-shadow: 0 4px 8px rgba(0,0,0,0.1); }
        .btn-warning { background: #ff9900; border: none; }
        .btn-warning:hover { background: #cc7700; }
        .modal-content { background-color: #fff; color: black; border-radius: 10px; }
        .btn-close { background-color: white; }
    </style>
</head>

<body>
    <nav class="navbar navbar-expand-lg navbar-dark">
        <div class="container">
            <a class="navbar-brand fw-bold" href="#"><img src="assets/images/OTTBUY-removebg-preview.png" alt="OTTBUY"></a>
            <div class="d-flex">
                <?php if (!$isLoggedIn) { ?>
                    <a href="login.php" class="btn btn-light me-2">Login</a>
                    <a href="register.php" class="btn btn-warning">Register</a>
                <?php } else { ?>
                    <a href="profile.php" class="btn btn-info me-2">Profile</a>
                    <a href="logout.php" class="btn btn-danger">Logout</a>
                <?php } ?>
            </div>
        </div>
    </nav>

    <div class="container mt-4">
        <button class="btn btn-success w-100 mb-3">Check Offers</button>

        <div class="row">
            <?php
            $sql = $conn->query("SELECT * FROM categorys");
            while ($data = $sql->fetch_assoc()) {
                $cat_id = $data['id'];
                $sql2 = $conn->query("SELECT * FROM sell WHERE category_id='$cat_id' AND status='1'");
                $count = mysqli_num_rows($sql2);
            ?>
                <div class="col-md-6 col-lg-4">
                    <div class="card mb-4 p-3 shadow-sm">
                        <div class="d-flex align-items-center mb-2">
                            <img src="<?php echo $data['logo_url']; ?>" class="rounded-circle border" height="55" width="55" style="margin-right:15px;">
                            <div class="flex-grow-1">
                                <h5 class="fw-bold mb-1"><?php echo $data['name']; ?></h5>
                                <p class="text-warning fw-bold">Stocks: <?php echo $count; ?></p>
                            </div>
                            <span class="badge bg-warning text-dark fs-5">₹<?php echo $data['amount']; ?></span>
                        </div>

                        <div class="text-center p-3 rounded bg-light text-dark">
                            <p><?php echo $data['info']; ?></p>
                        </div>

                        <div class="mt-3">
                            <?php if ($count > 0) { ?>
                                <a href="check.php?id=<?php echo $cat_id; ?>" class="btn btn-warning w-100">BUY NOW</a>
                            <?php } else { ?>
                                <button class="btn btn-danger w-100" disabled>OUT OF STOCK</button>
                            <?php } ?>
                        </div>
                    </div>
                </div>
            <?php } ?>
        </div>
    </div>

    <footer class="bg-dark text-white text-center py-4 mt-4">
        <div class="container">
            <p>&copy; 2025 OTTBUY - All Rights Reserved</p>
            <p>Follow us on <a href="https://t.me/ottbuyofficial" class="text-warning">Telegram</a></p>
        </div>
    </footer>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
